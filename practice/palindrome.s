	AREA RESET, DATA, READONLY
	EXPORT __Vectors
__Vectors
	DCD 0X10001000
	DCD Reset_Handler
	ALIGN
	AREA mycode, CODE, READONLY
	ENTRY
	EXPORT Reset_Handler
Reset_Handler
	LDR R0, =SRC		;FIRST BYTE
	MOV R8, #2			;BYTECOUNT
	ADD R1, R0, #3		;LAST BYTE
	MOV R7, #1			;RESULT

LOOP
	SUBS R8, R8, #1
	BLO DONE
	LDRB R2, [R0]		
	LDRB R3, [R1]
	ADD R0, R0, #1
	SUB R1, R1, #1
	
	AND R4, R2, #0X0F
	AND R5, R3, #0XF0
	LSR R5, #4
	CMP R4, R5
	MOVNE R7, #0
	BNE DONE
	
	AND R4, R2, #0XF0
	LSR R4, #4
	AND R5, R3, #0X0F
	CMP R4, R5
	MOVNE R7, #0
	BEQ LOOP
DONE 
	LDR R6, =DST
	STR R7, [R6]
STOP B STOP
SRC DCD 0X12345678
	AREA mydata, DATA, READWRITE
DST DCD 0